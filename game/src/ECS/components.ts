export type Position = {
    x: number,
    y: number
}

export type Velocity = {
    vx: number,
    vy: number
}

export type Force = {
    fx: number,
    fy: number
}

export type Renderable = {
    width: number,
    height: number,
    color: string
}

export type Entity = number
